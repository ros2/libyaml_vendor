# prevent multiple inclusion
if(_@YAML_NAME@_CONFIG_INCLUDED)
  return()
endif()
set(_@YAML_NAME@_CONFIG_INCLUDED TRUE)

# output package information
if(NOT @YAML_NAME@_FIND_QUIETLY)
  message(STATUS "Found @YAML_NAME@: @PACKAGE_VERSION@ (${@YAML_NAME@_DIR})")
endif()

# include all config extra files
set(_extras "ament_cmake_export_libraries-extras.cmake")
foreach(_extra ${_extras})
  include("${libyaml_vendor_DIR}/${_extra}")
endforeach()

set(yaml_LIBRARIES ${libyaml_vendor_LIBRARIES})
